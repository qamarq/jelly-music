package elovaire.music.droidbeauty.app.ui.theme

import androidx.compose.ui.graphics.Color

// Core app background for dark mode screens.
val Night = Color(0xFF000000)

// Shared card/module background in dark mode.
val Carbon = Color(0xFF101010)

// Secondary dark surface for chips, controls, and layered UI.
val Graphite = Color(0xFF181818)

// Main background for light mode screens.
val Cloud = Color(0xFFFFFFFF)

// Shared card/module background in light mode.
val Snow = Color(0xFFF7F7F8)

// Secondary light surface for chips, controls, and layered UI.
val Frost = Color(0xFFEDEDF0)

// Muted text or icon color in dark mode secondary states.
val Slate = Color(0xFFC2C2C8)

// Primary text color in light mode.
val InkText = Color(0xFF0D0D0D)

// Secondary text color in light mode.
val InkTextSecondary = Color(0xC20D0D0D)

// Global accent color for highlighted buttons, active states, and key actions.
val RoseAccent = Color(0xFF115FD5)

// Secondary accent reserved for About screen card-section buttons.
val AboutCardButtonAccent = Color(0xFF1D9BD7)

// Shared destructive action color for delete/remove actions.
val DestructiveRed = Color(0xFFDE2C2C)

// Dedicated green used by on/off setting toggles when enabled.
val ToggleEnabledGreen = Color(0xFF13D079)
